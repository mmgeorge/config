[package]
name = "forge-diff"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
license = "MIT"

[dependencies]
forge-buffer = { path = "../forge-buffer" }
imara-diff = { package = "gix-imara-diff", version = "=0.2.5", default-features = false }
memchr = "2"
sha2 = "0.10"
tokio = { version = "1.48", features = ["rt", "sync", "time"] }
tree-sitter = "=0.27.0"
tree-sitter-language = "=0.1.8"
regex = "1"

[build-dependencies]
cc = { version = "1", features = ["parallel"] }

[dev-dependencies]
tempfile = "3"
tokio = { version = "1.48", features = ["macros", "rt-multi-thread", "time"] }
